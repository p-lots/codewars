export const head = (arr: number[]) => arr[0];

export const tail = (arr: number[]) => arr.slice(1);

export const init = (arr: number[]) => arr.slice(0, -1);

export const last = (arr: number[]) => arr[arr.length - 1];