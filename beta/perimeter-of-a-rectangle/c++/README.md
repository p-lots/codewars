Implement method `getPerimeter` to calculate the perimeter of a rectangle using given `width` and `length`.

*You don't need to validate input parameters, all tests has valid arguments (positive integers)*.